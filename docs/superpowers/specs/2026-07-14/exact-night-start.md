# 夜间窗口精确启动设计

## 问题

“按窗口计划夜间诊断”原先使用 `setAndAllowWhileIdle`。在锁屏待机时，这类非精确闹钟可能被延后，不能保证用户设置的 00:00 会准时启动。

## 方案

- 已拥有“闹钟和提醒”特殊访问权时，使用 `setExactAndAllowWhileIdle` 调度每日开始事件。
- 无访问权或不支持该能力的系统，保留 `setAndAllowWhileIdle` 降级方案；用户界面的精确闹钟入口用于取得该访问权。
- 启动后，诊断服务内部负责五分钟采样和结束时刻；精确闹钟不用于高频周期采样。
