# 主备测速重试

主测速地址每轮最多尝试 5 次；每次失败后等待用户设置的重试间隔，默认 10 秒。主地址连续失败 5 次后，备用地址按同一规则最多尝试 5 次；主、备共 10 次均失败才写入完全失败结果。

每次真实 HTTP 尝试均须先预留单次测速预算。若预算不足，则停止后续尝试并记录 `BudgetExhausted`，不得为了重试突破夜间 1 GB 预算。重试等待运行于服务的后台测速线程，不能阻塞界面或快照调度。
