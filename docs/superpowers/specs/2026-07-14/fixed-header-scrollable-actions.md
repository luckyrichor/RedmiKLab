# 固定诊断摘要与可滚动操作区设计

## 问题

设置页的操作按钮随功能增加已超出一屏，底部操作会被系统导航区遮挡；整个页面滚动时，用户也会失去当前诊断窗口、测速地址和导出文件名的上下文。

## 方案

- 将标题、诊断窗口状态、测速地址输入框和保存文件名输入框置于固定的顶部区域。
- 将保存地址、时间设置、权限、开始/结束、导出和报告比较等操作按钮置入下方独立 `ScrollView`。
- 滚动区使用剩余高度，确保未来新增按钮仍可访问，顶部输入始终可见。
