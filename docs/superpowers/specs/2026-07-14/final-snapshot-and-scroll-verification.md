# 结束快照与操作区滚动设计

## 操作区

顶部的诊断摘要与输入框保持固定；按钮区必须在剩余高度内独立滚动。滚动条始终显示，避免小米系统的滚动条淡出使用户误以为页面不可滚动。临时加入六个无功能、禁用的占位按钮，以便在真机确认滚动边界与滚动条；验证后可移除。

## 结束快照

周期快照仅在结束时间之前调度，避免产生结束后的周期快照。收到结束事件时，无论上一条周期快照距结束时间是否不足五分钟，仍写入一条最终快照，再停止服务。最终快照的流量窗口仍为该时刻向前五分钟，可能与上一条窗口有重叠；它用于记录结束瞬间的网络状态，并非可与前一窗口简单累加的独立流量段。
