# 主动测速预算与端点容错实施计划

1. 用失败测试定义默认 1 GB 预算、HTTPS 备用端点，以及主端点失败后尝试备用端点的顺序。
2. 扩展诊断配置与偏好存储，保存备用地址和 1 GB 默认预算。
3. 实现独立的 `FailoverProbeRunner`，为每次尝试预留预算并返回首个成功结果。
4. 将诊断服务接入主/备用测速执行器，并在设置页增加备用地址输入框。
5. 执行测试、Lint、APK 构建并安装真机。
