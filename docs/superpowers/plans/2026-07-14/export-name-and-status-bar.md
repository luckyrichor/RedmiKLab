# 导出命名与状态栏修复实施记录

1. 为命名优先级和非法字符处理添加纯 Kotlin 单元测试。
2. 实现可测试的 `ReportFileNamePolicy`。
3. 在 SharedPreferences 中自动保存完整导出文件名，并在点开开始、结束时间按钮时记录对应时间已设置。
4. 在设置页接入可编辑的完整文件名输入框；导出直接使用输入框中的名称。
5. 从主题和 Activity 两层恢复普通系统状态栏。
6. 运行单元测试、Lint 和 debug APK 构建。
