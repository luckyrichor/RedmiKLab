# 锁屏定时可靠性、最终快照与安全滚动实施计划

1. 用失败测试定义同时收到过期快照与结束事件时只允许一次最终快照。
2. 实现 `FinalSnapshotGate`，让周期任务、结束任务和闹钟事件共用最终化入口。
3. 将五分钟周期与结束时刻加入受部分唤醒锁保护的前台服务 `Handler`；结束精确闹钟保留为兜底。
4. 增加电池优化豁免请求入口、删除滚动占位按钮，并按导航栏 inset 补足操作区底部间距。
5. 执行测试、Lint、APK 构建并安装至已连接手机。
