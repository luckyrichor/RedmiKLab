# RedmiKLab 项目约定

## 项目定位

RedmiKLab 是面向 Redmi K 系列设备的长期开发与测试项目。当前第一期是供 Redmi K60 与 K80 使用的 Android 移动网络夜间诊断 App；后续可增加 Android App、脚本、客户端和测试工具。

## 目录与文档

- 源码必须遵循其语言、构建系统和包管理器的标准目录结构；Android 源码位于 Gradle 标准的 `src/main`、`src/test` 等目录。
- 新建的文档、计划、规格、报告及其他归档性文件使用 `分类/YYYY-MM-DD/具体名称.扩展名`；日期目录采用 ISO 格式，文件名只描述具体内容，不重复日期。
- 新增或变更适合长期约束项目工作的规定时，同时更新本文件。

## 工程质量

- 生产逻辑实行测试先行：先写失败测试、确认失败原因、实现最小代码、再运行通过测试。
- 不直接在 `main` 分支开发；功能实现使用隔离工作区和功能分支。
- 每项独立、可验证的变更完成后提交 Git；提交前运行与改动相匹配的测试、构建或静态检查。
- 任何完成、可构建或测试通过的说明必须以当前命令输出为依据。
- 手机端视觉改版不得只依据代码常量或 Robolectric 标签判定完成；必须在目标 Redmi 真机截取主界面关键区域、操作区和至少一个弹窗，与已批准原型逐项核对后才能声明完成。
- 用户已授权持续推进已确认的实施计划：不得因阶段性提交或小任务完成而等待新的“继续”指令；只有需要新增权限、真机操作、外部协调，或出现无法安全自行判断的设计阻塞时才暂停询问。

## Android 第一期开关与边界

- 同一 APK 支持 K60 与 K80；不按机型分叉。
- 首期只诊断移动数据，不使用 Root 或常驻 ADB；连接级采集采用用户显式启用的本地 VPN，并必须与 Clash Meta 等其他 VPN 互斥。透明转发未完整验证前不得启用 VPN 接管流量。
- 用户可配置跨午夜运行窗口、采样间隔和流量预算。标准模式诊断窗口最长 6 小时并预留 120 秒安全收尾；严格模式依托连接级采集服务与有上限的 CPU 唤醒锁，可设置更长窗口。默认状态快照 5 分钟、连通性 10 分钟、吞吐 30 分钟、单次吞吐最多 5 MB、每晚最多 1 GB。
- 连接级采集不保存网络包负载、网页请求正文、账号凭证或视频二进制内容。用户已授权在诊断期间保存网络参与 App 与媒体相关 App 的完整可读取通知字段和媒体元数据；仅作为前台背景且无网络/媒体活动的 App 不保存完整通知。实现进度与真机证据持续记录在 `docs/superpowers/progress/YYYY-MM-DD/`。
- 夜间诊断提供“标准模式”和“严格模式”；采样间隔是独立可配置参数。两种模式都不得依赖连续运行数小时的 `dataSync` 服务；严格模式必须启用连接级采集，并用唤醒锁提高所选采样间隔的准时率。
- 数据默认仅保存在设备。导出的 ZIP 至少包含离线 HTML、CSV 和 JSON；报告中应用流量仅表达时段级关联，不能断言逐包或因果结论。
- 夜间诊断使用可见通知的前台服务；权限、电池限制、异常和数据缺失必须清楚记录。
